build/
data/*.idx
data/embeddings.bin
*.o
.DS_Store
compile_commands.json
dump.rdb

# Downloaded/exported model artifacts — large, regenerated by
# models/export_model.py, not source we wrote.
models/*.onnx
models/*.onnx.data
models/vocab.txt
